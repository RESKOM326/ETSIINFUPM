package aed.clase;

import es.upm.aedlib.Position;
import es.upm.aedlib.positionlist.PositionList;
import es.upm.aedlib.positionlist.NodePositionList;

@SuppressWarnings("unused")
public class RecPosition {
	
	/**
	 * Dada una lista de enteros devolver otra lista con 
	 * sus elementos elevados al cuadrado
	 */
	
	/**
	public PositionList<Integer> duplicar (PositionList<Integer> lista, Position<Integer> cursor) {
		if(lista==null || lista.isEmpty()) {
			return new NodePositionList<Integer>();
		}
		else {
			return (duplicar(lista, lista.next(cursor))).addFirst(2*cursor.element());
		}
	}
	
	*/
}
